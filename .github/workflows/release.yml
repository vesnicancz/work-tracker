name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  DOTNET_VERSION: '10.0.x'
  CONFIGURATION: Release

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore -c ${{ env.CONFIGURATION }}

      - name: Test
        run: dotnet test --no-build -c ${{ env.CONFIGURATION }}

  publish-cli:
    name: Publish CLI (${{ matrix.rid }})
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - rid: win-x64
            artifact: WorkTracker-CLI-win-x64
          - rid: linux-x64
            artifact: WorkTracker-CLI-linux-x64
          - rid: osx-x64
            artifact: WorkTracker-CLI-osx-x64
          - rid: osx-arm64
            artifact: WorkTracker-CLI-osx-arm64
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish
        run: >
          dotnet publish src/WorkTracker.CLI/WorkTracker.CLI.csproj
          -c ${{ env.CONFIGURATION }}
          -r ${{ matrix.rid }}
          -p:SelfContained=false
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:DebugType=none
          -o publish/${{ matrix.artifact }}

      - name: Archive
        run: |
          cd publish/${{ matrix.artifact }}
          zip -r ../${{ matrix.artifact }}.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: publish/${{ matrix.artifact }}.zip

  publish-wpf:
    name: Publish WPF
    needs: test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish
        run: >
          dotnet publish src/WorkTracker.WPF/WorkTracker.WPF.csproj
          -c ${{ env.CONFIGURATION }}
          -r win-x64
          -p:SelfContained=false
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:DebugType=none
          -o publish/WorkTracker-WPF-win-x64

      - name: Archive
        run: Compress-Archive -Path publish/WorkTracker-WPF-win-x64/* -DestinationPath publish/WorkTracker-WPF-win-x64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: WorkTracker-WPF-win-x64
          path: publish/WorkTracker-WPF-win-x64.zip

  publish-avalonia:
    name: Publish Avalonia (${{ matrix.rid }})
    needs: test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - rid: win-x64
            os: windows-latest
            artifact: WorkTracker-Avalonia-win-x64
          - rid: linux-x64
            os: ubuntu-latest
            artifact: WorkTracker-Avalonia-linux-x64
          - rid: osx-x64
            os: macos-latest
            artifact: WorkTracker-Avalonia-osx-x64
          - rid: osx-arm64
            os: macos-latest
            artifact: WorkTracker-Avalonia-osx-arm64
          - rid: win-arm64
            os: windows-latest
            artifact: WorkTracker-Avalonia-win-arm64
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish
        run: >
          dotnet publish src/WorkTracker.Avalonia/WorkTracker.Avalonia.csproj
          -c ${{ env.CONFIGURATION }}
          -r ${{ matrix.rid }}
          -p:SelfContained=false
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:DebugType=none
          -o publish/${{ matrix.artifact }}

      - name: Archive (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cd publish/${{ matrix.artifact }}
          zip -r ../${{ matrix.artifact }}.zip .

      - name: Archive (Windows)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path publish/${{ matrix.artifact }}/* -DestinationPath publish/${{ matrix.artifact }}.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: publish/${{ matrix.artifact }}.zip

  publish-plugins:
    name: Publish Plugin (${{ matrix.plugin }})
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - plugin: Atlassian
            project: plugins/WorkTracker.Plugin.Atlassian/WorkTracker.Plugin.Atlassian.csproj
            artifact: WorkTracker.Plugin.Atlassian
          - plugin: Luxafor
            project: plugins/WorkTracker.Plugin.Luxafor/WorkTracker.Plugin.Luxafor.csproj
            artifact: WorkTracker.Plugin.Luxafor
          - plugin: GoranG3
            project: plugins/WorkTracker.Plugin.GoranG3/WorkTracker.Plugin.GoranG3.csproj
            artifact: WorkTracker.Plugin.GoranG3
          - plugin: Office365Calendar
            project: plugins/WorkTracker.Plugin.Office365Calendar/WorkTracker.Plugin.Office365Calendar.csproj
            artifact: WorkTracker.Plugin.Office365Calendar
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish
        run: >
          dotnet publish ${{ matrix.project }}
          -c ${{ env.CONFIGURATION }}
          -p:DebugType=none
          -o publish/${{ matrix.artifact }}

      - name: Archive
        run: |
          cd publish
          zip -r ${{ matrix.artifact }}.zip ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: publish/${{ matrix.artifact }}.zip

  release:
    name: Create GitHub Release
    needs: [publish-cli, publish-wpf, publish-avalonia, publish-plugins]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: artifacts/*.zip
