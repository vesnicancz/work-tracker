<?xml version="1.0" encoding="UTF-8"?>
<!--
  Applied only when signing with a real Developer ID (see make-app-bundle.sh), because the
  hardened runtime that notarization requires would otherwise kill the app: the CoreCLR JIT
  needs writable-executable memory, and the single-file host extracts native libraries that
  carry no signature of their own.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
